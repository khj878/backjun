#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

int N;
string S1, S2;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	cin >> S1 >> S2;
	bool flag = true;
	bool toggle = false;
	int cnt = 0;

	for (int i = 0; i < S1.length(); i++) {

	}

	return 0;
}